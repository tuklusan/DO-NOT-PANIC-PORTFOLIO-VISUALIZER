powershell -NoProfile -Command "$logDir=Join-Path $env:LOCALAPPDATA 'PortfolioSaver\logs'; if(Test-Path $logDir){ $latest=Get-ChildItem $logDir -File | Sort-Object LastWriteTime -Descending | Select-Object -First 2; $latest | Select-Object FullName,LastWriteTime,Length | Format-Table -AutoSize; foreach($f in $latest){ Write-Output ('--- ' + $f.FullName); Get-Content -Path $f.FullName -Tail 120 } }"

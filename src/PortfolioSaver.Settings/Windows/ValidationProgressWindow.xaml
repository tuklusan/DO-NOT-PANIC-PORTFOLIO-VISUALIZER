<Window x:Class="PortfolioSaver.Config.Windows.ValidationProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Validation Progress"
        Icon="/Assets/Branding/dnppv-icon-rev-3.ico"
        Width="500"
        Height="260"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="#111111">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Validating symbols and providers..."
                   Foreground="#F4FAFF"
                   FontSize="16"
                   FontWeight="SemiBold"
                   Margin="0,0,0,8" />

        <TextBox Grid.Row="1"
                 x:Name="LogTextBox"
                 IsReadOnly="True"
                 Text="{Binding ValidationLogText, Mode=OneWay}"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Disabled"
                 FontFamily="Consolas"
                 FontSize="12"
                 Background="#191919"
                 Foreground="#D8E7F4"
                 BorderBrush="#3A4753"
                 BorderThickness="1"
                 Padding="10"
                 TextChanged="OnLogTextChanged" />
    </Grid>
</Window>
